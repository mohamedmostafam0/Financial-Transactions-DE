version: 2

sources:
  - name: raw_data
    project: sunlit-pixel-456910-j0
    dataset: raw
    tables:
      - name: raw_transactions
        description: "Raw transaction data ingested from MinIO."
      - name: raw_economic_indicators
        description: "Raw economic indicators data ingested from FRED API."
